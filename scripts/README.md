# Automation Scripts — Planned

Future scripts should support:
- local security preflight;
- SBOM/AI-BOM generation;
- signature/provenance verification;
- OpenShift preflight/deploy/verify;
- adversarial demo execution;
- evidence bundling;
- security result normalization.

Every script must return non-zero on a blocking failure and print a machine-readable or unambiguous status.
