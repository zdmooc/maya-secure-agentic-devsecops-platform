# OpenShift Runtime Workstream

Status: **PLANNED**

Target environment: OpenShift Local / CRC first.

Proof sequence:
1. deploy synthetic demo;
2. prove service accounts/RBAC;
3. prove NetworkPolicy default deny;
4. prove Kyverno negative admission;
5. prove externalized secrets;
6. prove observability;
7. prove MCP/tool denial and HITL;
8. prove signed-artifact admission if supported by selected policy path.

CRC evidence remains local-lab only.
